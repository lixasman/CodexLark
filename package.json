{
  "name": "codexlark-feishu",
  "version": "0.1.0",
  "private": true,
  "description": "Feishu long-connection Codex control service.",
  "license": "MIT",
  "type": "commonjs",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:setup": "tsc -p tsconfig.json",
    "build:test": "node ./scripts/run-node-tests.cjs --build-only",
    "test": "node ./scripts/run-node-tests.cjs",
    "start": "node dist/agent-cli.js feishu-longconn"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.8.0"
  }
}
